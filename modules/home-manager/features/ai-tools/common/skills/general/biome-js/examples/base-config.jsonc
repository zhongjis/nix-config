// Minimal Biome configuration for a library project
// Extends shared config and provides required file patterns
{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "extends": ["@org/biome-config"],
  "files": {
    "includes": ["**/*.{js,json,jsonc,ts}", "!node_modules/**/*"]
  },
  "overrides": [
    {
      "includes": ["**/tests/**/*.ts"],
      "linter": {
        "rules": {
          "style": {
            "noNonNullAssertion": "off"
          }
        }
      }
    }
  ]
}
