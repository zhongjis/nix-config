{...}: {
  # OpenCode agents
  programs.opencode.agents = {
    code-simplifier = ./general-code-simplifier.md;
  };

  # Claude Code agents
  programs.claude-code.agents = {
    code-simplifier = ./general-code-simplifier.md;
  };
}
