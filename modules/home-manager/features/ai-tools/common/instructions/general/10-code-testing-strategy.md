# Code Testing Strategy

Use inspected execution evidence when it still covers the relevant final state. Repeat checks for invalidation, missing coverage, diagnosis, or explicit policy—not merely a delegation or phase boundary. Preserve required gates and account for mutable external state.
