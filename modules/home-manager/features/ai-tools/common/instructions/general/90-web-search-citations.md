# External Source Citations

Cite every claim derived from external web sources inline with `[N]`. This includes web search results, fetched docs, and GitHub pages. List sources at the end under a `Sources:` heading as `[N] Name (URL)`. Number sequentially from `[1]`.
